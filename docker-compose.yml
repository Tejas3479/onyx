version: '3.8'

services:
  onyx:
    build: .
    image: onyx:latest
    container_name: onyx_server
    ports:
      - "8000:8000"
    environment:
      - API_KEYS=mykey1,mykey2
      - MAX_PLAYWRIGHT_INSTANCES=3
      - SESSION_TTL_MINUTES=30
      - MAX_SESSIONS=100
      - REDIS_URL=redis://redis:6379/0
      - DATABASE_URL=postgresql+asyncpg://onyx:onyx@postgres:5432/onyx
    volumes:
      - data_volume:/app/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: onyx_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    container_name: onyx_postgres
    environment:
      POSTGRES_USER: onyx
      POSTGRES_PASSWORD: onyx
      POSTGRES_DB: onyx
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U onyx"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  worker:
    build: .
    image: onyx:latest
    container_name: onyx_worker
    command: arq worker.WorkerSettings
    environment:
      - REDIS_URL=redis://redis:6379/0
      - DATABASE_URL=postgresql+asyncpg://onyx:onyx@postgres:5432/onyx
    volumes:
      - data_volume:/app/data
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    restart: unless-stopped

volumes:
  redis_data:
  postgres_data:
  data_volume:
